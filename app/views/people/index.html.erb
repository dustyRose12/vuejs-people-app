<div id="app">

<div>
  Search Term: <input v-model="searchTermFilter" list="names">
  <datalist id="names">
    <option v-for="person in modifiedPeople">{{ person.name }}</option>
  </datalist>

  <button v-on:click="setSortAttribute('name')">Sort by Name</button>
  <button v-on:click="setSortAttribute('bio')">Sort by Bio</button>
</div>

  <h4>Add a Person:</h4>
  <ul>
    <li v-for="error in errors">{{ error }}</li>
  </ul>
  <div>
    Enter Name:  <input v-model="newPersonName">
  </div>
 <div>
   Enter Bio:  <input v-model="newPersonBio">
 </div>

 <button v-on:click="addPerson()">Add Person</button>


  <h2>People</h2>
<!--  <transition-group name="fade">
  <div v-for="person in people" v-if="isValidPerson(person)" v-bind:key="person.id">
    <h4>Name: {{ person.name }} <button v-on:click="deletePerson(person)">Delete</button> </h4>
    <h5 v-if="{strike: person.bioVisible}">Bio: {{ person.bio}}</h5> <! you can bind another style class to this v-bind:class, in this case strike is the one you're binding if bioVisible is true. So it does 2 things, it toggles it from the toggleBio call, and it strikes it also 
    <br>
</div>
</transition-group> -->

  

  <transition-group name="zoomDown">
    <div v-for="person in people" v-if="isValidPerson(person)" v-bind:key="person.id">
      <h2 v-on:click="toggleBio(person)">{{ person.name }}</h2> 
      <div v-if="person.bioVisible">
        <h3>{{ person.bio }}</h3>
        <h6 v-on:click="deletePerson(person)">Delete</h6>
      </div>
    </div>
  </transition-group>


<div >
  Total Number of People: {{ people.length }}
</div>



</div>

<!-- ====================================
 -->

  



